import React from 'react';
import LeavesList from '../default/components/LeaveManagement/LeavesList';

const index = () => {
  return (
    <React.Fragment>
      <LeavesList />
    </React.Fragment>
  );
};

export default index;
